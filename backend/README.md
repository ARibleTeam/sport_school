# Backend Cifra API

FastAPI приложение для проекта Cifra.

## 🚀 Быстрый старт

### 🐳 Запуск с Docker (Рекомендуется)

```bash
# Клонируйте репозиторий
git clone <repository-url>
cd backend_cifra

# Скопируйте файл окружения
cp env.example .env

# Запустите в режиме разработки (с автоперезагрузкой)
docker-compose -f docker-compose.dev.yml up --build

# Или запустите в продакшн режиме
docker-compose up --build
```

### 💻 Локальная разработка

```bash
# Установка зависимостей
python -m ensurepip --upgrade
pip install -r requirements.txt

# Запуск приложения
python main.py
```

### Доступ к API

После запуска приложение будет доступно по адресу:
- **API**: http://localhost:8000
- **Документация Swagger**: http://localhost:8000/docs
- **Документация ReDoc**: http://localhost:8000/redoc

## 📋 Доступные эндпоинты

### Основные
- `GET /` - Корневой эндпоинт
- `GET /health` - Проверка состояния API

### CRUD операции для товаров
- `GET /items` - Получить список всех товаров
- `GET /items/{item_id}` - Получить товар по ID
- `POST /items` - Создать новый товар
- `PUT /items/{item_id}` - Обновить товар
- `DELETE /items/{item_id}` - Удалить товар

## 🛠 Технологии

- **FastAPI** - Современный, быстрый веб-фреймворк для создания API
- **Uvicorn** - ASGI сервер для запуска FastAPI
- **Pydantic** - Валидация данных и сериализация
- **Python-dotenv** - Управление переменными окружения

## 📁 Структура проекта

```
backend_cifra/
├── app/                    # Основная папка приложения
│   ├── __init__.py        # Инициализация пакета
│   ├── schemas/           # Pydantic схемы
│   │   ├── __init__.py
│   │   ├── common.py      # Общие схемы
│   │   └── item.py        # Схемы для товаров
│   └── routers/           # Маршруты API
│       ├── __init__.py
│       ├── common.py      # Общие эндпоинты
│       └── items.py       # Эндпоинты для товаров
├── main.py                # Точка входа для Docker
├── app/main.py           # Основной файл приложения
├── requirements.txt       # Зависимости Python
├── env.example           # Пример файла переменных окружения
├── .gitignore            # Игнорируемые файлы Git
└── README.md             # Документация проекта
```

## 🔧 Настройка

### Docker настройка

1. Скопируйте `env.example` в `.env`:
   ```bash
   cp env.example .env
   ```

2. Отредактируйте `.env` файл под ваши нужды

3. Запустите FastAPI приложение:
   ```bash
   # Для разработки (с автоперезагрузкой)
   docker-compose -f docker-compose.dev.yml up --build
   
   # Для продакшна
   docker-compose up --build
   ```

### Локальная настройка

1. Установите зависимости:
   ```bash
   pip install -r requirements.txt
   ```

2. Запустите приложение:
   ```bash
   python main.py
   ```

## 🐳 Docker команды

### Основные команды

```bash
# Запуск в режиме разработки
docker-compose -f docker-compose.dev.yml up --build

# Запуск в продакшн режиме
docker-compose up --build

# Остановка контейнеров
docker-compose down

# Просмотр логов
docker-compose logs -f api

# Пересборка только API
docker-compose build api

# Запуск в фоновом режиме
docker-compose up -d
```

### Управление контейнером

```bash
# Просмотр логов
docker-compose logs -f api

# Остановка контейнера
docker-compose down

# Пересборка образа
docker-compose build api

# Запуск в фоновом режиме
docker-compose up -d
```

## 🧪 Тестирование

Для тестирования API используйте:
- Swagger UI: http://localhost:8000/docs
- ReDoc: http://localhost:8000/redoc
- Или любой HTTP клиент (Postman, curl, etc.)

### Примеры запросов

#### Создание товара
```bash
curl -X POST "http://localhost:8000/items" \
     -H "Content-Type: application/json" \
     -d '{
       "name": "Тестовый товар",
       "description": "Описание товара",
       "price": 100.50,
       "is_active": true
     }'
```

#### Получение всех товаров
```bash
curl -X GET "http://localhost:8000/items"
```

## 📝 Разработка

### Добавление новых эндпоинтов

1. Создайте Pydantic модели для валидации данных
2. Добавьте функции-обработчики с декораторами FastAPI
3. Обновите документацию

### Архитектура проекта

Проект организован по принципу разделения ответственности:

- **`app/schemas/`** - Pydantic схемы для валидации и сериализации данных
- **`app/routers/`** - Маршруты API, сгруппированные по функциональности
- **`main.py`** - Точка входа приложения, настройка FastAPI и подключение роутеров

### Структура для будущего развития

- `app/models/` - Модели базы данных (SQLAlchemy)
- `app/services/` - Бизнес-логика
- `app/database/` - Настройки базы данных
- `app/auth/` - Аутентификация и авторизация
- `app/middleware/` - Пользовательские middleware
- `tests/` - Тесты

## 🤝 Вклад в проект

1. Форкните репозиторий
2. Создайте ветку для новой функции
3. Внесите изменения
4. Создайте Pull Request

## 📄 Лицензия

Этот проект лицензирован под MIT License.
